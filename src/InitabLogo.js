import React from "react";
import Logo, { ReactComponent as IniLogo } from "./Logo.svg";

const InitabLogo = () => (
  <div style={{}}>
    <image style={{ backgroundSize: "50px" }}>
      <IniLogo />
    </image>
  </div>
);
export default InitabLogo;
